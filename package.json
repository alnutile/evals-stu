{
  "name": "stu-evals",
  "version": "0.1.0",
  "private": true,
  "description": "promptfoo eval harness to score and iterate the Bellwether report-generation prompt.",
  "scripts": {
    "eval": "promptfoo eval -c promptfooconfig.yaml",
    "selftest": "node harness/selftest.js",
    "view": "promptfoo view -y",
    "eval:view": "promptfoo eval -c promptfooconfig.yaml && promptfoo view -y"
  },
  "dependencies": {
    "csv-parse": "^5.6.0"
  },
  "devDependencies": {
    "promptfoo": "latest"
  }
}
